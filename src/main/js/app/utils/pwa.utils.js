define([], function() {
    var fakeFunction = function () {

    };

    return {
        addListener: fakeFunction,
        sendMessage: fakeFunction,
        setAuthHeader: fakeFunction,
        getAuthHeader: fakeFunction,
        createNotification: fakeFunction,
        getPraxisVersion: fakeFunction,
        getOS: fakeFunction,
        init: fakeFunction
    };
});
