define([], function() {
    return function($scope) {
        var init = function() {
            $scope.message = "hi there, welcome to state of the art offline approval system.";
        };

        init();
    };
});
